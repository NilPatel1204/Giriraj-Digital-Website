---
const {BgImage, Heading="", Paragraph=""} = Astro.props;
---

<div class="p-4 rounded-[20px] bg-cover  h-fit outline-8 outline-[rgba(18,158,216,0.2)] text-white" style={`background-image: url('${BgImage.src}');`}>
    <h4 class="font-bold lg:text-[56px] leading-[100%]">{Heading || ""}</h4>
    <p class="font-semibold">{Paragraph || ""}</p>
</div>

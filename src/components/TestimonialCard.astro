---
const {TestimonialImage, Content=[], Name, Designation} = Astro.props;
import TestimonialSays from "../assets/images/testimonial-says.svg"
---

<div class="grid grid-cols-10 gap-5 lg:gap-[30px]">
    <div class="col-span-12 lg:col-span-3 aspect-square p-8 h-fit lg:p-[60px] bg-[url('../assets/images/testimonial-bg.webp')] rounded-4xl">
        <img src={TestimonialImage.src} class="border-6 border-[rgba(255,255,255,0.6)] rounded-[20px] aspect-square! h-fit w-[320px]" alt={Name}>
    </div>
    <div class="col-span-12 lg:col-span-7 content-center text-[#2C363A] space-y-2.5 text-lg md:text-xl lg:text-2xl lg:px-5 xl:px-10">
        <div class="space-y-6">
            <img src={TestimonialSays.src} alt="testimonial says">
            {Content.map((text:any) => (
                <p>{text}</p>
            ))}
        </div>
        <div class="space-y-1">
            <p class="font-bold">{Name || ""}</p>
            <p>{Designation || ""}</p>
        </div>
    </div>
</div>